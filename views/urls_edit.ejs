<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit URL</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/urls">TinyApp</a>
      <div class="d-flex">
        <% if (user) { %>
          <span class="navbar-text me-3">Logged in as <%= user.email %></span>
          <form action="/logout" method="POST">
            <button type="submit" class="btn btn-outline-danger">Logout</button>
          </form>
        <% } else { %>
          <a href="/login" class="btn btn-outline-primary me-2">Login</a>
          <a href="/register" class="btn btn-outline-success">Register</a>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <h3>Edit URL</h3>
    <form action="/urls/<%= url.shortURL %>" method="POST">
      <div class="mb-3">
        <label for="longURL" class="form-label">New Long URL:</label>
        <input type="text" class="form-control" id="longURL" name="longURL" value="<%= url.longURL %>" required>
      </div>
      <button type="submit" class="btn btn-primary">Save Changes</button>
    </form>
  </div>
</body>
</html>
